# Changelog

## 0.15.0 — 2026-08-04

### Added

- 新增多语言设置：源语言支持自动检测或手动指定，目标语言可由用户选择。
- 接入 Chrome 扩展语言检测、Chrome Language Detector API 和脚本特征回退，自动检测使用悬停所在句子的上下文，不直接依赖单个短词判断语言。
- 本地翻译缓存现在按引擎、源语言、目标语言和原文共同区分，避免不同语言对互相复用译文。
- 设置页新增 Chrome Translator API 支持的语言列表和当前语言对状态提示。

### Changed

- 默认源语言从英语改为自动检测；旧版本首次升级时自动迁移为自动检测。
- 本地翻译器会按实际检测到的源语言和用户选择的目标语言创建语言对。

### Upgrade notes

- 运行 `npm run build` 后，在 `chrome://extensions` 中重新加载 `dist`，再刷新网页。
- Chrome 本地翻译的语言对和语言包由浏览器按需提供；未提供的语言对会显示明确提示，不会静默显示空白。

## 0.14.2 — 2026-08-04

### Changed

- 悬停候选改为“内容词优先单词级翻译”，只有冠词、介词、连词等功能词才补充必要上下文。
- 删除按句子长度均分 2–7 词的策略，局部短语最多 4 个词，减少相邻内容词被误合并造成的翻译错乱。
- 悬停单词不再受全局 `minLength: 6` 限制，像 `most`、`year` 这样的短内容词也可以被翻译。

### Upgrade notes

- 运行 `npm run build` 后，在 `chrome://extensions` 中重新加载 `dist`，再刷新网页。

## 0.14.1 — 2026-08-04

### Fixed

- 悬停路径现在强制使用鼠标命中的单词附近短语，不再受旧设置中 `sentence/paragraph` 值影响。
- 升级时自动把旧的悬停单位迁移为 `phrase`，避免继续走整句布局而在单词位置显示空白。
- 设置页明确显示“单词附近语义短语”，并补充 `assistant` 等演示短语的本地演示译文。

### Upgrade notes

- 运行 `npm run build` 后，在 `chrome://extensions` 中重新加载 `dist`。
- 刷新网页后，按 `Alt/Option` 开启墨迹，再把鼠标停在单词上等待约 220ms。

## 0.14.0 — 2026-08-04

### Changed

- 悬停翻译改为只响应鼠标中心实际命中的文字，不再使用墨迹周边采样点触发邻近短语。
- 单词和短语译文改为使用命中原文行的局部布局，不再使用整段内容宽度居中排版。
- 跨行短语只清理当前命中行和译文实际占用的行，避免原文被遮住而译文落在墨迹之外。
- 新增候选命中点缓存，异步翻译完成后仍使用最初命中的原文行进行布局。
- 默认悬停等待调整为 220ms，离开文字后的结果保留时间调整为 160ms。

### Upgrade notes

- 运行 `npm run build` 后，在 `chrome://extensions` 中重新加载 `dist`。
- 如果浏览器中仍保留旧的悬停等待设置，可在设置页将“悬停等待”调整为 220ms 左右。
