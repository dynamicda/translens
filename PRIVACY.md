# 译镜 TransLens 隐私说明

更新日期：2026-08-04

译镜会在普通 HTTP/HTTPS 网页中自动载入，以便刷新页面后无需先打开插件即可响应 `Alt/Option` 和鼠标手势。自动载入不会读取整页正文；只有鼠标附近、满足翻译条件的文字会被处理。输入框、密码框、代码区和可编辑区域始终跳过。

默认翻译由 Chrome 本地 Translator API 在设备上完成。译镜自身不把网页文字、浏览历史、Cookie、账号信息或翻译记录上传到服务器，也不出售或用于广告。扩展只在 `chrome.storage.local` 中保存功能设置、本地模型状态和网站启用/禁用规则。

用户可以在插件弹窗中全局关闭译镜或单独禁用当前网站，也可以在 Chrome 扩展管理页撤销网站访问权限或清除扩展数据。卸载扩展会删除由 Chrome 保存的扩展本地数据。

当前版本不提供云翻译。未来若加入需要上传文字的翻译服务，必须在发送任何文字前明确说明接收方、用途和数据范围，并取得用户主动同意。
