{
  "name": "translens",
  "version": "0.15.0",
  "private": true,
  "description": "译镜 TransLens：用悬停墨斑或拖选笔迹原位揭开下层译文。",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run build"
  }
}
